<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1a1a1a"
    tools:context=".presentation.intro.IntroActivity">

    <!-- Chalkboard texture overlay -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_chalkboard" />

    <!-- Words Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp">

        <!-- Word 1: TRACK -->
        <TextView
            android:id="@+id/word1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:alpha="0"
            android:fontFamily="@font/chalk_font"
            android:letterSpacing="0.15"
            android:text="TRACK"
            android:textColor="#FFFFFF"
            android:textSize="64sp"
            android:textStyle="bold" />

        <!-- Word 2: SAVE -->
        <TextView
            android:id="@+id/word2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:alpha="0"
            android:fontFamily="@font/chalk_font"
            android:letterSpacing="0.15"
            android:text="SAVE"
            android:textColor="#FFFFFF"
            android:textSize="64sp"
            android:textStyle="bold" />

        <!-- Word 3: GROW -->
        <TextView
            android:id="@+id/word3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:alpha="0"
            android:fontFamily="@font/chalk_font"
            android:letterSpacing="0.15"
            android:text="GROW"
            android:textColor="#FFFFFF"
            android:textSize="64sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Chalk dust particles (decorative) -->
    <View
        android:id="@+id/chalkDust"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0"
        android:background="@drawable/bg_chalk_dust" />

</FrameLayout>
